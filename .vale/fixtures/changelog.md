# Changelog

This file seeds one breach of every rule in the Changelog style. The self-test,
`bin/test-lint-docs`, fails when any rule stays silent on it. It is not the
changelog and is never fixed.

## [Unreleased]

- `aw status` now prints the age of the last fetch (#12)
- `aw sync` prints the age of the last fetch
- `aw doctor` names a missing tool (#14) and exits with status 1
